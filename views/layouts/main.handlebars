<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tutor Datty</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav>
        <h1>Tutor Datty</h1>
        <div id="rightnav">

            <input onclick="window.location.href = window.location.origin + '/login';" class="btn rightnav" id="login-btn"
                type="button" value="{{#if loggedIn}}Homepage{{else}}Homepage{{/if}}">

            {{#if loggedIn}}
            <input onclick="window.location.href = window.location.origin + '/reviews';" class="btn rightnav" id="make-review"
                type="button" value="Make a Review">
            {{else}}
            {{/if}}

            <input class="rightnav" id="searchbar" type="text" placeholder="Search tutor here">

            <input class="btn rightnav" id="logout-btn" type="button" value="Logout">

            {{#if loggedIn}}
            <input onclick="window.location.href = window.location.origin + '/chat';" class="btn rightnav chat-button"
                id="chat-btn" type="button" value="ChatGPT">
            {{else}}
            {{/if}}
        </div>
    </nav>

    <main>
        {{{body}}}
    </main>

    <script src="/js/logout.js"></script>
</body>

</html>